---
name: stratisd nightly

on:
  schedule:
    - cron: 15 3 * * *

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  jobs:
    strategy:
      matrix:
        include:
          - task: cargo package
            components: cargo
    runs-on: ubuntu-24.04
    container:
      image: fedora:43  # CURRENT DEVELOPMENT ENVIRONMENT
    steps:
      - name: Install dependencies for Fedora
        run: >
          dnf install -y
          clang
          curl
          cryptsetup-devel
          device-mapper-devel
          git
          libblkid-devel
          just
          ncurses
          openssl-devel
          python-requests
          python-semantic_version
          systemd-devel
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: ${{ matrix.components }}
      - name: Test ${{ matrix.task }} on CURRENT DEVELOPMENT RUST TOOLCHAIN
        run: ${{ matrix.task }}

  check_future_dependencies:
    runs-on: ubuntu-24.04
    container:
      image: fedora:43  # CURRENT DEVELOPMENT ENVIRONMENT
    steps:
      - name: Install dependencies for Fedora
        run: >
          dnf install -y
          clang
          cryptsetup-devel
          curl
          device-mapper-devel
          glibc-static
          git
          libblkid-devel
          just
          ncurses
          openssl-devel
          systemd-devel
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: cargo
      - uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-audit
      - name: Run cargo-update
        run: cargo update
      - name: Build all targets
        run: just -f justfile build-all-rust
      - name: Run cargo-audit
        run: just -f justfile audit

  cargo-audit-checks:
    continue-on-error: true
    strategy:
      matrix:
        include:
          - task: just -f justfile audit
          - task: AUDITABLE=1 PROFILEDIR=debug just -f justfile audit-all-rust
    runs-on: ubuntu-24.04
    container:
      image: fedora:43  # CURRENT DEVELOPMENT ENVIRONMENT
    steps:
      - name: Install dependencies for Fedora
        run: >
          dnf install -y
          clang
          curl
          cryptsetup-devel
          git
          glibc-static
          libblkid-devel
          just
          ncurses
          openssl-devel
          python-requests
          python-semantic_version
          systemd-devel
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: cargo
      - uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-audit
      - uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-auditable
      - name: Run matrix task
        run: ${{ matrix.task }}

  checks-with-ci-repo:
    runs-on: ubuntu-24.04
    container:
      image: fedora:43  # CURRENT DEVELOPMENT ENVIRONMENT
    steps:
      - name: Install dependencies for Fedora
        run: >
          dnf install -y
          clang
          curl
          cryptsetup-devel
          device-mapper-devel
          git
          libblkid-devel
          just
          ncurses
          openssl-devel
          python-requests
          python-semantic_version
          systemd-devel
      - uses: actions/checkout@v6
        with:
          path: stratisd
          persist-credentials: false
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: cargo
          rust-src-dir: stratisd
      - name: Check out ci repo
        uses: actions/checkout@v6
        with:
          path: ci
          repository: stratis-storage/ci
          persist-credentials: false
      - name: Run comparisons of macro version specs with Fedora packages
        run: |
          COMPARE_FEDORA_VERSIONS=$GITHUB_WORKSPACE/ci/dependency_management/compare_fedora_versions FEDORA_RELEASE=rawhide IGNORE_ARGS="--ignore-high=libcryptsetup-rs --ignore-high=libcryptsetup-rs-sys --ignore-high=devicemapper --ignore-high=libblkid-rs" just -f ../Makefile check-fedora-versions
          COMPARE_FEDORA_VERSIONS=$GITHUB_WORKSPACE/ci/dependency_management/compare_fedora_versions FEDORA_RELEASE=f44 IGNORE_ARGS="--ignore-high=libcryptsetup-rs --ignore-high=libcryptsetup-rs-sys --ignore-high=devicemapper --ignore-high=libblkid-rs" just -f ../Makefile check-fedora-versions
          COMPARE_FEDORA_VERSIONS=$GITHUB_WORKSPACE/ci/dependency_management/compare_fedora_versions FEDORA_RELEASE=f43 IGNORE_ARGS="--ignore-high=libcryptsetup-rs --ignore-high=libcryptsetup-rs-sys --ignore-high=devicemapper --ignore-high=libblkid-rs" just -f ../Makefile check-fedora-versions
        working-directory: stratisd/stratisd_proc_macros
      - name: Run comparisons of version specs with available Fedora packages
        run: |
          COMPARE_FEDORA_VERSIONS=$GITHUB_WORKSPACE/ci/dependency_management/compare_fedora_versions FEDORA_RELEASE=rawhide IGNORE_ARGS="--ignore-high=libcryptsetup-rs --ignore-high=libcryptsetup-rs-sys --ignore-high=devicemapper --ignore-high=libblkid-rs" just -f justfile check-fedora-versions
          COMPARE_FEDORA_VERSIONS=$GITHUB_WORKSPACE/ci/dependency_management/compare_fedora_versions FEDORA_RELEASE=f44 IGNORE_ARGS="--ignore-high=libcryptsetup-rs --ignore-high=libcryptsetup-rs-sys --ignore-high=devicemapper --ignore-high=libblkid-rs" just -f justfile check-fedora-versions
          COMPARE_FEDORA_VERSIONS=$GITHUB_WORKSPACE/ci/dependency_management/compare_fedora_versions FEDORA_RELEASE=f43 IGNORE_ARGS="--ignore-high=libcryptsetup-rs --ignore-high=libcryptsetup-rs-sys --ignore-high=devicemapper --ignore-high=libblkid-rs" just -f justfile check-fedora-versions
        working-directory: stratisd

  python-based-tests:
    runs-on: ubuntu-24.04
    steps:
      - name: Run apt-get update
        run: sudo apt-get -q update
      - name: Install dependencies
        run: >
          DEBIAN_FRONTEND=noninteractive
          sudo apt-get install -y
          asciidoc
          clang
          curl
          git
          libblkid-dev
          libcryptsetup-dev
          libdevmapper-dev
          libsystemd-dev
          libudev-dev
          just
          pkg-config
          thin-provisioning-tools
          udev
          xfsprogs
      - name: Install Python dependencies
        run: >
          sudo python -m pip install
          dbus-python
          dbus-client-gen
          dbus-python-client-gen
          justbytes
          psutil
          pyudev
          tenacity
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: cargo
      - name: Build stratisd
        run: PROFILEDIR=debug just -f justfile build-all
      - name: Install stratisd
        run: sudo just PROFILEDIR=debug -f Makefile install
      - name: Workaround for dbus inotify
        run: sudo cp stratisd.conf /usr/share/dbus-1/system.d
      - name: Reload udev
        run: sudo udevadm control --reload
      - name: Run some tests in client-dbus
        run: >
          sudo just
          RUST_LOG=stratisd=debug
          STRATISD=/usr/libexec/stratisd
          STRATIS_PREDICT_USAGE=/usr/bin/stratis-predict-usage
          PYTHONPATH=./src
          predict-tests
        working-directory: ./tests/client-dbus
