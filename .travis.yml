os: linux
group: edge

addons:
    apt:
        packages:
          - libudev-dev

language: rust

matrix:
    include:
        - rust: stable
          sudo: required
          env: TASK=build-no-default TARGET=x86_64-unknown-linux-gnu

branches:
    only: master

script: make -f Makefile $TASK
